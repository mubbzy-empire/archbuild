import fs from 'node:fs'; import vm from 'node:vm';
const src=fs.readFileSync(new URL('./frontend/src/three/architecture/phase34Systems.js',import.meta.url),'utf8').replace(/export /g,''); const sandbox={}; vm.createContext(sandbox); vm.runInContext(src+';globalThis.api={normalizePhase34,generatePhase34Package,validatePhase34,PHASE34_SCHEMA};',sandbox);
const b={id:'coord-test',phase32:{deliverables:[{id:'qa-report',status:'ready'}]},phase31:{baseline:{revision:1}},phase33:{exports:[{format:'ifc4',status:'ready'}]},levels:[{index:1,walls:[{id:'W1',start:[0,0],end:[5,0],thickness:.2,height:3,openings:[{id:'D1',type:'door',width:1,height:2}]}],rooms:[{id:'R1',name:'Living',polygon:[[0,0],[5,0],[5,4],[0,4]],floorFinish:'tile'}]}]}; const m=sandbox.api.generatePhase34Package(b); const v=sandbox.api.validatePhase34(b); if(!v.valid) throw new Error(v.errors.join('; ')); if(m.schedules.rooms[0].areaM2!==20) throw new Error('room schedule mismatch'); if(m.status!=='ready') throw new Error('package should be ready'); console.log(JSON.stringify({schema:sandbox.api.PHASE34_SCHEMA,rooms:m.schedules.rooms.length,openings:m.schedules.openings.length,status:m.status,validation:v.valid?'passed':'failed'},null,2));
