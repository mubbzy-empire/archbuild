import fs from 'node:fs'; import vm from 'node:vm';
const src=fs.readFileSync(new URL('./frontend/src/three/architecture/phase32Systems.js',import.meta.url),'utf8').replace(/export /g,''); const sandbox={}; vm.createContext(sandbox); vm.runInContext(src+';globalThis.api={normalizePhase32,generatePhase32Documentation,validatePhase32,phase32Manifest,PHASE32_SCHEMA};',sandbox);
const b={id:'doc-test',phase30:{status:'passed'},phase31:{baseline:{revision:1,label:'Issued'}},levels:[{index:1,walls:[{id:'W1',start:[0,0],end:[5,0],thickness:.2,height:3,openings:[]}],rooms:[{id:'R1',polygon:[[0,0],[5,0],[5,4],[0,4]]}]}]};
const d=sandbox.api.generatePhase32Documentation(b,{projectName:'Doc Test'}); const v=sandbox.api.validatePhase32(b); if(!v.valid) throw new Error(v.errors.join('; ')); if(d.schedule.totalRoomAreaM2!==20) throw new Error('area schedule mismatch'); console.log(JSON.stringify({schema:sandbox.api.PHASE32_SCHEMA,sheets:d.sheets.length,deliverables:d.deliverables.length,area:d.schedule.totalRoomAreaM2,status:v.valid?'passed':'failed'},null,2));
