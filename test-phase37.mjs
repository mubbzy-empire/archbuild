import fs from 'node:fs';import vm from 'node:vm';
const src=fs.readFileSync(new URL('./frontend/src/three/architecture/phase37Systems.js',import.meta.url),'utf8').replace(/export /g,'');const sandbox={};vm.createContext(sandbox);vm.runInContext(src+';globalThis.api={generatePhase37Integrity,validatePhase37,PHASE37_SCHEMA};',sandbox);
const b={id:'hardening-test',metadata:{schema:'archvision-bim-1.26'},phase30:{},phase36:{release:{ready:true}},levels:[{index:1,walls:[{id:'W1',start:[0,0],end:[5,0],openings:[{id:'D1'}]}],rooms:[{id:'R1',polygon:[[0,0],[5,0],[5,4],[0,4]]}]}]};const m=sandbox.api.generatePhase37Integrity(b);const v=sandbox.api.validatePhase37(b);if(!v.valid||!v.ready||!m.integrity.hash)throw new Error(v.errors.join('; ')||'Phase 37 mismatch');console.log(JSON.stringify({schema:sandbox.api.PHASE37_SCHEMA,hash:m.integrity.hash,status:m.status,validation:'passed'},null,2));
