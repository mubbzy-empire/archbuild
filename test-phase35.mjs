import fs from 'node:fs';import vm from 'node:vm';const src=fs.readFileSync(new URL('./frontend/src/three/architecture/phase35Systems.js',import.meta.url),'utf8').replace(/export /g,'');const sandbox={};vm.createContext(sandbox);vm.runInContext(src+';globalThis.api={generatePhase35Presentation,validatePhase35,PHASE35_SCHEMA};',sandbox);const b={id:'presentation-test',levels:[{index:1,walls:[{id:'W1',start:[0,0],end:[5,0]}]}],phase32:{deliverables:[{id:'docs',status:'ready'}]}};const m=sandbox.api.generatePhase35Presentation(b);const v=sandbox.api.validatePhase35(b);if(!v.valid||m.views.length!==2||m.status!=='ready')throw new Error(v.errors.join('; ')||'Phase 35 mismatch');console.log(JSON.stringify({schema:sandbox.api.PHASE35_SCHEMA,views:m.views.length,status:m.status,validation:'passed'},null,2));
