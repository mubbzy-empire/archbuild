import * as THREE from 'three';
export function buildPhase23SpaceTopologyGroup(building){const root=new THREE.Group();root.name='phase23_space_topology';root.userData.group='phase23';const mat=new THREE.LineBasicMaterial({color:0x8ba7ff,transparent:true,opacity:.8});for(const level of building.levels||[])for(const room of level.rooms||[]){if(!room.polygon?.length)continue;const pts=room.polygon.map(p=>new THREE.Vector3(p[0],level.elevation+.045,p[1]));pts.push(pts[0]);const line=new THREE.Line(new THREE.BufferGeometry().setFromPoints(pts),mat);line.userData={group:'phase23',roomId:room.id,floor:level.index,spaceBoundary:true};root.add(line);}return root;}
