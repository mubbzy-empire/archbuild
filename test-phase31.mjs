import fs from 'node:fs'; import vm from 'node:vm';
const src=fs.readFileSync(new URL('./frontend/src/three/architecture/phase31Systems.js',import.meta.url),'utf8').replace(/export /g,''); const sandbox={}; vm.createContext(sandbox); vm.runInContext(src+';globalThis.api={normalizePhase31,capturePhase31Baseline,createPhase31Revision,comparePhase31,validatePhase31,phase31Manifest,PHASE31_SCHEMA};',sandbox);
const b={id:'rev-test',levels:[{index:1,walls:[{id:'W1',start:[0,0],end:[5,0],thickness:.2,height:3,openings:[]}],rooms:[{id:'R1',polygon:[[0,0],[5,0],[5,4],[0,4]]}]}]};
sandbox.api.normalizePhase31(b); sandbox.api.capturePhase31Baseline(b,{label:'Issued'}); b.levels[0].walls.push({id:'W2',start:[5,0],end:[5,4],thickness:.2,height:3,openings:[]}); const r=sandbox.api.createPhase31Revision(b,{label:'Revision 2',author:'architect',note:'Added wall'}); if(r.changeSet.counts.added!==1) throw new Error('expected one added element'); const v=sandbox.api.validatePhase31(b); if(!v.valid) throw new Error(v.errors.join('; ')); console.log(JSON.stringify({schema:sandbox.api.PHASE31_SCHEMA,revision:b.phase31.revision,added:r.changeSet.counts.added,history:b.phase31.history.length},null,2));
