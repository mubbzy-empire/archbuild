import fs from 'node:fs'; import vm from 'node:vm';
const src=fs.readFileSync(new URL('./frontend/src/three/architecture/phase33Systems.js',import.meta.url),'utf8').replace(/export /g,''); const sandbox={}; vm.createContext(sandbox); vm.runInContext(src+';globalThis.api={normalizePhase33,generatePhase33ExportManifest,validatePhase33,PHASE33_SCHEMA};',sandbox);
const b={id:'interop-test',levels:[{index:1,walls:[{id:'W1',start:[0,0],end:[5,0],thickness:.2,height:3,openings:[{id:'D1',type:'IfcDoor',width:1,height:2}]}],rooms:[{id:'R1',polygon:[[0,0],[5,0],[5,4],[0,4]]}]}]}; const m=sandbox.api.generatePhase33ExportManifest(b); const v=sandbox.api.validatePhase33(b); if(!v.valid) throw new Error(v.errors.join('; ')); if(m.entities.length!==3) throw new Error('entity manifest mismatch'); console.log(JSON.stringify({schema:sandbox.api.PHASE33_SCHEMA,entities:m.entities.length,exports:m.exports.length,status:v.valid?'passed':'failed'},null,2));
